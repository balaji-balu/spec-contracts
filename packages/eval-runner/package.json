{
  "name": "eval-runner",
  "version": "1.0.0",
  "description": "Eval scorer (evals/scoring.md) and the k-run suite runner for spec-contracts agent steps.",
  "type": "module",
  "private": true,
  "bin": {
    "score": "bin/score.js",
    "run-suite": "bin/run-suite.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --import tsx --test --test-force-exit test/*.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "ajv": "^8.17.1",
    "spec-lint": "*",
    "step-runner": "*",
    "tsx": "^4.19.2",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
