`synthetic-judge-output.json` is **hand-written**, not produced by a model. It exists only to test
`score.py judge`, the checker that compares judge scores with the expected verdicts in `case.yaml`.
Real judge outputs come from the eval-judge pi session during a suite run.
